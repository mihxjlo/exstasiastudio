apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: exstasia
type: Opaque
stringData:
  DB_PASSWORD: "CHANGE_ME"
  ADMIN_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME_generate_with: node -e \"require('crypto').randomBytes(32).toString('base64')\""
